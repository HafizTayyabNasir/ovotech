export const metadata = {
  title: 'Ovotech - Third Page (Draft)',
  description: 'Internal draft restyle for Ovotech presentation sections.',
  robots: 'noindex, nofollow'
};

export default function ThirdLayout({ children }) {
  return children;
}

